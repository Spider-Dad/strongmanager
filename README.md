# GetCourse Telegram Bot

**Версия: 2.0.0 (PostgreSQL + n8n)**

Телеграм-бот для оповещения наставников онлайн школы Strong Manager о действиях студентов в платформе GetCourse.

---

## ✅ Новая архитектура (после рефакторинга)

### Технологический стек

- **База данных:** PostgreSQL 17.6 на Amvera
- **Обработка вебхуков:** n8n workflows → PostgreSQL
- **Бизнес-логика:** Python 3.11+ сервисы
- **Уведомления:** Прямая отправка через Telegram Bot API
- **Планировщик:** APScheduler для периодических задач

### Компоненты системы

```
GetCourse → n8n → PostgreSQL → Python Services → Telegram Bot → Менторы
```

1. **GetCourse** отправляет вебхуки о событиях (новый ответ, комментарий)
2. **n8n** валидирует и записывает в таблицу `webhook_events`
3. **WebhookProcessingService** обрабатывает вебхуки каждые 30 сек
4. **DeadlineCheckService** проверяет дедлайны каждый час
5. **ReminderService** создает напоминания раз в день (12:00 MSK)
6. **NotificationSenderService** отправляет уведомления каждые 15 сек
7. **Telegram Bot** доставляет уведомления менторам

### Преимущества новой архитектуры

✅ **Надежность:** PostgreSQL + транзакции + retry механизмы
✅ **Производительность:** Асинхронная обработка, батчинг, connection pooling
✅ **Масштабируемость:** Легко добавить новые типы уведомлений
✅ **Мониторинг:** Детальное логирование, статистика в БД
✅ **Независимость:** Нет зависимости от Google Sheets и Apps Script

---

## Функциональность

### Для наставников

- **Авторизация** по email (привязка Telegram к справочнику PostgreSQL)
- **Уведомления о новых ответах** студентов на задания
- **Уведомления о дедлайнах** (за 36 часов, если студент не ответил)
- **Ежедневные напоминания** о непроверенных ответах (12:00 MSK)
- **Табель успеваемости** студентов (опционально через `GRADEBOOK_ENABLED`)

### Для администраторов

- **Доступ ко всем функциям** наставников
- **Статистика по всем наставникам** (в табеле)
- **Синхронизация данных** с Google Sheets (опционально)

---

## Логика работы (новая)

### 1. Обработка вебхуков (каждые 30 сек)

```python
webhook_events (processed=false)
  → WebhookProcessingService
  → find mentor через mapping
  → create notification (status=pending)
  → update webhook (processed=true)
```

### 2. Проверка дедлайнов (каждый час)

```python
lessons (deadline < now + 36h)
  → DeadlineCheckService
  → find students without answers
  → group by mentors
  → check duplicates
  → create notifications (status=pending)
```

### 3. Отправка уведомлений (каждые 15 сек)

```python
notifications (status=pending)
  → NotificationSenderService
  → get mentor telegram_id
  → send to Telegram
  → update status (sent/failed/no_telegram_id)
```

### 4. Напоминания (раз в день 12:00 MSK)

```python
webhook_events (date = now - 2 days, status=new)
  → ReminderService
  → group by mentors
  → create reminder notifications (status=pending)
```

### Обработка ошибок

Бот использует продвинутую систему обработки сетевых ошибок:

- **Exponential backoff** — автоматические повторы с увеличивающимися задержками
- **Circuit breaker** — предотвращение каскадных сбоев при проблемах с API
- **Jitter** — случайность в задержках для предотвращения одновременных запросов
- **Graceful degradation** — бот продолжает работать даже при временных проблемах с сетью
- **Retry для уведомлений** — повторные попытки отправки уведомлений при сетевых ошибках (ClientConnectorError, ServerDisconnectedError, Gateway Timeout и др.)

#### Система retry для уведомлений

При возникновении сетевых ошибок при отправке уведомлений бот автоматически:
1. Повторяет отправку с увеличивающимися задержками (exponential backoff)
2. Использует jitter для предотвращения одновременных запросов
3. Помечает уведомление как `failed` только после исчерпания всех попыток
4. Логирует каждую неудачную попытку для мониторинга

Это значительно снижает потери уведомлений при нестабильной сети.

---

## Типы уведомлений

- **answerToLesson** — студент отправил новый ответ на урок
- **commentOnLesson** — студент оставил комментарий к уроку
- **deadlineApproaching** — студент не дал ответ, а до дедлайна менее 24 часов

---

## Настройка и запуск

### Переменные окружения

Создайте файл `.env` на основе `.env.example` и заполните необходимые параметры:

- `BOT_TOKEN` — токен Telegram-бота
- `API_URL` — адрес API Google Apps Script (web app)
- `ADMIN_EMAIL` — email администратора (для логов)
- `ADMIN_IDS` — список Telegram ID администраторов через запятую (например: 123456789,987654321)
- `POLLING_INTERVAL` — интервал опроса уведомлений в секундах (по умолчанию: 15)
- `SERVER_ENV` — окружение (dev/prod)

### Настройки таймаутов и retry

Для настройки обработки сетевых ошибок и автоматических повторов:

- `HTTP_TIMEOUT` — общий таймаут HTTP запросов в секундах (по умолчанию: 30)
- `HTTP_CONNECT_TIMEOUT` — таймаут установки соединения в секундах (по умолчанию: 10)
- `MAX_RETRIES` — максимальное количество попыток для API запросов (по умолчанию: 3)
- `RETRY_BASE_DELAY` — базовая задержка между попытками в секундах (по умолчанию: 1.0)
- `RETRY_MAX_DELAY` — максимальная задержка между попытками в секундах (по умолчанию: 60.0)
- `SYNC_MAX_RETRIES` — максимальное количество попыток для синхронизации (по умолчанию: 3)
- `SYNC_RETRY_BASE_DELAY` — базовая задержка для синхронизации в секундах (по умолчанию: 2.0)
- `SYNC_RETRY_MAX_DELAY` — максимальная задержка для синхронизации в секундах (по умолчанию: 120.0)

### Настройки retry для уведомлений

Для настройки повторных попыток отправки уведомлений при сетевых ошибках:

- `NOTIFICATION_MAX_RETRIES` — максимальное количество попыток отправки уведомления (по умолчанию: 3)
- `NOTIFICATION_RETRY_BASE_DELAY` — базовая задержка между попытками в секундах (по умолчанию: 2.0)
- `NOTIFICATION_RETRY_MAX_DELAY` — максимальная задержка между попытками в секундах (по умолчанию: 60.0)

**Рекомендуемые настройки для нестабильной сети:**
```
NOTIFICATION_MAX_RETRIES=5
NOTIFICATION_RETRY_BASE_DELAY=2.0
NOTIFICATION_RETRY_MAX_DELAY=300.0
```

### Запуск

```bash
python main.py
```

---

## Система алертов

Бот включает систему автоматических алертов для администраторов при возникновении ошибок:

### Функциональность
- Автоматическая отправка уведомлений администраторам при ошибках уровня ERROR
- Предотвращение спама: одинаковые ошибки отправляются не чаще чем раз в 5 минут
- Сохранение истории последних 20 ошибок
- Команда `/alerts` для управления системой алертов

### Команды администратора
- `/alerts` — меню управления системой алертов
  - 📊 Последние ошибки — просмотр истории ошибок
  - 🔔 Тест алерта — генерация тестовой ошибки
  - ℹ️ Статус системы — информация о состоянии системы
- `/sync` — меню управления синхронизацией БД
  - 🔄 Синхронизировать сейчас — запуск ручной синхронизации
  - 📊 Статус синхронизации — информация о последней синхронизации
  - ⚙️ Настройки — просмотр текущих настроек

### Настройка
Для активации системы алертов добавьте в `.env`:
```
ADMIN_IDS=123456789,987654321
```
где числа — это Telegram ID администраторов, которые будут получать алерты.

---

## Синхронизация базы данных

Бот поддерживает автоматическую и ручную синхронизацию локальной БД SQLite с Google Sheets:

### Функциональность
- Автоматическая синхронизация по расписанию (настраивается через переменную окружения)
- Ручная синхронизация через команду администратора
- История синхронизаций с информацией о количестве синхронизированных записей
- Отслеживание статуса и ошибок синхронизации

### Команды администратора

**УДАЛЕНО:** `/sync` - синхронизация с Google Sheets больше не используется

После миграции на PostgreSQL все справочные данные (mentors, students, trainings, lessons, mapping) обновляются напрямую через DBeaver.

---

## Развертывание на сервере

### Необходимые файлы (не включены в репозиторий)

1. **Переменные окружения**: `.env`
   - Создайте на основе `env.example`
   - Заполните все необходимые значения
   - **Обязательные:** `BOT_TOKEN`, `POSTGRES_*` параметры

### Процесс развертывания

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd getcourse_bot

# 2. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Создайте файл .env и заполните переменные
cp env.example .env
# Отредактируйте .env (обязательно: BOT_TOKEN, POSTGRES_* параметры)

# 5. Инициализируйте базу данных PostgreSQL
python db/init_database.py

# 6. Заполните справочные данные через DBeaver
# (mentors, students, trainings, lessons, mapping)

# 7. Запустите бота
python main.py
```

---

## Управление данными

### Справочные данные (обновляются вручную через DBeaver)

- **mentors** — наставники (telegram_id заполняется после регистрации)
- **students** — студенты
- **trainings** — тренинги
- **lessons** — уроки (с deadline_date)
- **mapping** — связи студент-ментор-тренинг

### Автоматически заполняемые данные

- **webhook_events** — вебхуки от GetCourse (через n8n)
- **notifications** — уведомления для отправки (через Python сервисы)
- Для корректной работы требуется, чтобы у наставника был указан Telegram ID в справочнике mentors
